services:
  - type: web
    name: electric-vehicles
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
    envVars: []
    root: .
